Class {
	#name : #APMultilineTextPrompt,
	#superclass : #APTextPrompt,
	#category : #'APart BasicParts'
}

{ #category : #'initialize-release' }
APMultilineTextPrompt >> defaultPromptSelector [

	^#textMultiline:
]
