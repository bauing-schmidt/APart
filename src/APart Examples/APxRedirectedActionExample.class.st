"
Beispiel-Views werden selbst nicht kommentiert, sondern nur ihre jeweils zugehörige Part-Klasse (zu finden in #matchingPartClass).
"
Class {
	#name : #APxRedirectedActionExample,
	#superclass : #APartExampleGUI,
	#instVars : [
		'lastRemoved',
		'lastRemovingKey'
	],
	#category : #'APart Examples-RedirectedAction'
}

{ #category : #API }
APxRedirectedActionExample class >> matchingPartClass [

	^APxRedirectedActionExamplePart
]

{ #category : #aspects }
APxRedirectedActionExample >> lastRemoved [
	"This method was generated by UIDefiner.  Any edits made here
	may be lost whenever methods are automatically defined.  The
	initialization provided below may have been preempted by an
	initialize method."

	^lastRemoved isNil
		ifTrue:
			[lastRemoved :=  APartPlatform current nativeValueHolderFrom: String new]
		ifFalse:
			[lastRemoved]
]

{ #category : #aspects }
APxRedirectedActionExample >> lastRemovingKey [
	"This method was generated by UIDefiner.  Any edits made here
	may be lost whenever methods are automatically defined.  The
	initialization provided below may have been preempted by an
	initialize method."

	^lastRemovingKey isNil
		ifTrue:
			[lastRemovingKey :=  APartPlatform current nativeValueHolderFrom: String new]
		ifFalse:
			[lastRemovingKey]
]

{ #category : #announcements }
APxRedirectedActionExample >> removedSomething: aTestAnnouncement announcer: aSubInterface [

	(self presenterNamed: #lastRemoved) text: aTestAnnouncement what.
	(self presenterNamed: #lastRemovingKey) text: aTestAnnouncement flag		

]
